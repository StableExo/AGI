# =============================================================
# Gemini Citadel: Environment Configuration v0.1
# Copy this file to .env and fill in the values.
# NEVER commit the .env file to version control.
# =============================================================

# 1. CORE APPLICATION CONFIGURATION
NODE_ENV=development

# 2. BLOCKCHAIN PROVIDER & RPC CONFIGURATION
# --- API Keys for RPC Providers (future use) ---
ALCHEMY_API_KEY=""
INFURA_API_KEY=""

# --- HTTP RPC ENDPOINTS ---
# The primary RPC URL the DataService will connect to.
ARBITRUM_RPC_URL="https://arb-mainnet.g.alchemy.com/v2/${ALCHEMY_API_KEY}"

# --- WEBSOCKET (WSS) ENDPOINTS (future use) ---
ARBITRUM_WSS_URL="wss://arb-mainnet.g.alchemy.com/v2/${ALCHEMY_API_KEY}"